#include <iostream>
#include <cstring>
#include <cmath>

using namespace std;

int main()
{
double r1, r2, h1, h2, v1, v2;
string input;
cin >> input;
int firstc = input.find(",");
int secondc = input.find((","), firstc + 1);
int thirdc = input.find((","), secondc + 1);
r1 = stod(input.substr(0, firstc)) / 10;
h1 = stod(input.substr(firstc + 1 , secondc - firstc - 1)) / 10;
r2 = stod(input.substr(secondc + 1, thirdc - secondc - 1)) / 10;
h2 = stod(input.substr(thirdc + 1)) / 10;
v1 = 3.1415 * r1 * r1 * h1;
v2 = 3.1415 * r2 * r2 * h2;
if(v1 > v2) cout << v1 << endl;
else if(v1 < v2) cout << v2 << endl;
else cout << v1 << endl;
    return 0;
}
